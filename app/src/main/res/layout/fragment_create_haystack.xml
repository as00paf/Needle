<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.nemator.needle.home.createHaystack.CreateHaystackFragment">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- ViewPager -->
        <android.support.v4.view.ViewPager
            xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/create_haystack_view_pager"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white"
            android:layout_alignParentTop="true"/>

        <!-- Bottom Sheet -->
        <RelativeLayout
            android:id="@+id/new_haystack_bottom_sheet"
            android:layout_width="match_parent"
            android:layout_height="84dp"
            android:layout_alignParentBottom="true"
            android:elevation="8dp"
            android:background="@android:color/white">

            <!-- View Pager Indicator -->
            <com.viewpagerindicator.CirclePageIndicator
                android:id="@+id/view_pager_indicator"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:layout_marginTop="32dp"
                android:layout_centerHorizontal="true"
                android:background="@android:color/white"
                app:radius="12dp"
                app:fillColor="@color/primary"
                app:pageColor="#999"
                app:strokeColor="@android:color/transparent"
                app:strokeWidth="1dp"/>

            <!-- Back Button -->
            <Button
                android:id="@+id/create_haystack_back_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/back_button_label"
                style="?android:attr/borderlessButtonStyle"
                android:gravity="center"
                android:layout_alignParentBottom="true"
                android:layout_margin="4dp"
                android:layout_marginBottom="6dp"
                android:enabled="false"/>

            <!-- Next Button -->
            <Button
                android:id="@+id/create_haystack_next_button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/next_button_label"
                style="?android:attr/borderlessButtonStyle"
                android:gravity="center"
                android:layout_alignParentBottom="true"
                android:layout_alignParentRight="true"
                android:layout_marginBottom="6dp"
                android:layout_margin="4dp"/>
        </RelativeLayout>
    </RelativeLayout>

    <!-- FAB -->
    <com.shamanland.fab.FloatingActionButton
        android:id="@+id/new_haystack_photo_fab"
        android:elevation="4dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_marginRight="16dp"
        android:layout_marginBottom="60dp"
        android:src="@drawable/ic_photo_camera_black_24dp"/>
</RelativeLayout>